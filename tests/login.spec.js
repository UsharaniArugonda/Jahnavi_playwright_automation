// import {test, expect} from '@playwright/test'

// test ('login scenario', async ({page})=>{
// await page.goto('https://www.debenhams.com'); // navigate to the website 
// await page.pause();
// //const context = await browser.newContext();

// await page.getByRole('button', { name: 'ACCEPT ALL' }).click(); //accepts the cookies 
// await page.locator('[test-id= "account-link"]').click(); // clicks the account icon
// await page.locator('input[name="username"]').click(); // clicks on the username textbox field 
// await page.locator('input[name="username"]').fill('jahnavibalu@gmail.com'); // enters the username 
// await page.locator('input[name="password"]').click(); // clicks on the password field 
// await page.locator('input[name="password"]').fill('Welcome123!'); //  fills in the password 
// await page.locator('[data-test-id="login-button"]').click();  // logs in by clicking the login button 

// // ---------------------
// //await context.close();
// //await browser.close();
// })


