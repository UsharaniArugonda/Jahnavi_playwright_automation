
// import {test, expect} from '@playwright/test';
// import {LoginPage} from '../src/pom/LoginPage';


// Test Suite - Login to Debehnams website with valid credentials.
//  test('debenhams login', async ({ page}) => {
  
// // Create a new instance of the Login Page

// const LoginPage = new LoginPage(page) 

// LoginPage.baseURL();
// LoginPage.clickAcceptCookies();
// LoginPage.clickAccountIcon();
// LoginPage.submitLoginDetails;

// });

//     await page.goto("https://www.debenhams.com"); //go to the tedbaker website  
//     await expect(page).toHaveTitle("Fashion, Home & Beauty | Debenhams"); //checking if the title of the webpage
//     await page.getByRole('button', { name: 'ACCEPT ALL' }).click(); // accept cookies
//      // click on account to login    
//     await page.locator('[data-test-id="account-link"]').click();
//     // login to the account - enter username and password 
//     await page.locator('[data-test-id="login-button"]').click();
//     await page.locator('input[name="username"]').click();
//     await page.locator('input[name="username"]').fill('jahnavibalu@gmail.com');
//     await page.locator('input[name="password"]').click();
//     await page.locator('input[name="password"]').fill('Welcome123!');
//     await page.locator('input[name="password"]').press('Enter');
//     await browser.close();// close the window
    
 
      


